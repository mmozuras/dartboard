script(type='text/javascript')
  $(function() {
    $('ul.ui-sortable').sortable({ 
      connectWith: 'ul.ui-sortable', 
      items: 'li:not(.ui-state-disabled)' 
    });

    function movableWithDblClick(from, to) {
      $(from).live('dblclick', function() {
        $(this).hide().appendTo($(to)).fadeIn();
      });
    };

    movableWithDblClick('ul#players > li', 'ul#selected-players');
    movableWithDblClick('ul#selected-players > li', 'ul#players');
  });

ul#players(class='ui-sortable')
  li.ui-state-disabled Players
  - for (var player in players)
    li.ui-state-default
      =players[player].name

ul#selected-players(class='ui-sortable')
  li.ui-state-disabled Selected players
